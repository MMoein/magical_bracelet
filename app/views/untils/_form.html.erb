<%= form_for(@until) do |f| %>
  <% if @until.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@until.errors.count, "error") %> prohibited this until from being saved:</h2>

      <ul>
      <% @until.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rule_id %><br>
    <%= f.number_field :rule_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
